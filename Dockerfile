# Используйте официальный образ Python как родительский образ
FROM python:3.12

# Установите рабочую директорию в контейнере
WORKDIR /app

# Скопируйте содержимое локальной директории в рабочую директорию контейнера
COPY . /app

# Установите пакеты, указанные в файле requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Сообщите Docker, что контейнер будет слушать на порту 5000
EXPOSE 5000

# Запустите app.py при запуске контейнера
CMD ["flask", "run", "--host=0.0.0.0"]
